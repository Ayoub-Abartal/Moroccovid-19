import 'package:flutter/material.dart';

class Items {
  String title;
  String subtitle;
  String img;
  int regn;
  Widget widget;
  double fs;
  Items({this.title, this.widget, this.subtitle, this.img, this.fs, this.regn});
}
